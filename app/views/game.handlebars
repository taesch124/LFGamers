<div class="row">
  
  <div class="col s12">
    <div class="card" data-id="{{id}}">

      <div class="row flex">
        <div class="col s3">
            <img src="{{poster}}" alt="{{name}} poster">
        </div>
        <div class="col s9">
          <h3><strong>{{name}}</strong></h4>
          <p>{{summary}}</p>
        </div>
      </div>

    </div>
  </div>

</div>